/*
 * meteolast.c c -- get the most recent update to the database and format it
 *                  in an XML based format
 * 
 * (c) 2002 Dr. Andreas Mueller, Beratung und Entwicklung
 */
#ifdef HAVE_CONFIG_H
#include <config.h>
#endif
#include <meteo.h>
#include <mysql.h>
#ifdef HAVE_STDLIB_H
#include <stdlib.h>
#endif
#ifdef HAVE_STDIO_H
#include <stdio.h>
#endif

int	main(int argc, char *argv[]) {
	fprintf(stderr, "IMPLEMENTATION OF meteolast MISSING\n");
	exit(EXIT_SUCCESS);
}
